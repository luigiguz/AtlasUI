# AtlasVPN — interfaz y logos
customtkinter>=5.2.0
Pillow>=10.0.0

# UI web (python -m atlas_api) + ventana de escritorio sin navegador externo
fastapi>=0.115.0
itsdangerous>=2.1.0
uvicorn[standard]>=0.32.0
pywebview>=5.0
argon2-cffi>=23.1.0
PyJWT>=2.8.0
asyncssh>=2.14.0

# Cliente oficial Cloudflare (útil para scripts/API; la sincronización de AtlasVPN usa REST v4 con el mismo token)
cloudflare>=3.1.0

# Gestión de tiendas (atlas-stores / fleet.yaml)
PyYAML>=6.0.0

# Base de datos (PostgreSQL en Docker; SQLite local sin ATLAS_DATABASE_URL)
SQLAlchemy>=2.0.0
psycopg[binary]>=3.1.0
alembic>=1.13.0
