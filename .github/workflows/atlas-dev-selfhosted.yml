# DESACTIVADO: el run #45 quedó en cola sin runner y GitHub no permite cancelarlo.
# Usar atlas-dev-deploy.yml (workflow "Atlas — dev deploy (RPi)").

name: Atlas — dev build y deploy (self-hosted) [legacy]

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow legacy desactivado. Usa Atlas — dev deploy (RPi)."
